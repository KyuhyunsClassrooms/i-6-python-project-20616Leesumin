# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제:편의점 꿀조합 음식 추천 및 칼로리 계산기

예) 기분과 시간에 따른 활동 추천기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

예) 사용자가 현재 기분과 사용할 수 있는 시간을 입력하면, 조건에 맞는 활동을 추천해 주고 싶다.

내 설명:친구들과 편의점에서 음식을 조합해 먹을 때, 내가 가진 돈(예산) 안에서 살 수 있으면서 내 목적(다이어트 식단 또는 든든하고 헤비한 한 끼 등)에 맞는 꿀조합 메뉴를 찾기 어렵다. 또한, 매번 고른 메뉴들의 총 가격과 칼로리를 일일이 계산하기 번거롭다는 문제가 있다. 이를 해결하기 위해 사용자의 예산과 원하는 식단 스타일을 입력받아 딱 맞는 편의점 꿀조합을 골라주고, 총 칼로리와 구매 후 남는 잔액까지 자동으로 계산해 주는 프로그램을 만들고자 한다.

---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 사용자가 키보드로 입력하는 '현재 예산(정수형 숫자)'

- 사용자가 키보드로 입력하는 '원하는 식단 스타일(문자열: 다이어트 / 헤비)'

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 프로그램 내부에 저장된 2차원 리스트(편의점 메뉴 창고)를 반복문으로 한 줄씩 읽는다.

- 조건문을 사용하여 음식의 가격이 사용자의 예산보다 작거나 같고(<=), 음식의 식단 분류가 사용자가 입력한 스타일과 일치하는지(==) 두 조건을 and 연산자로 동시에 비교하여 검사한다.

- 조건에 맞는 메뉴가 있다면 사용자의 예산에서 음식 가격을 빼서 '남는 잔액'을 계산하는 산술 연산을 처리한다.

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 조건에 만족하는 편의점 꿀조합의 이름, 가격, 칼로리를 출력한다.

- 예산에서 해당 메뉴 가격을 차감하고 남은 '잔액(원)'을 화면에 계산하여 출력한다.

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:`outfits`

예) foods, movies, characters, questions, records

```python
# 예시 형식
convenience_store = [
    ["마크정식(떡볶이+스파게티+치즈)", 6500, 1200, "헤비"],
    ["닭가슴살 샐러드+바나나 세트", 4800, 350, "다이어트"],
    ["불닭김밥+스트링치즈 조합", 3900, 680, "헤비"],
    ["훈제계란과 두유 세트", 3200, 250, "다이어트"]
]
```

### 한 행의 의미
편의점 꿀조합 메뉴 1개의 전체 정보 (조합 이름, 가격, 칼로리, 식단 분류)

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
|

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| :--- | :--- | :--- | :--- |
| `show_intro()` | 프로그램의 시작을 알리는 제목을 예쁘게 출력 | 없음 | 화면 출력 |
| `get_temperature()` | 사용자에게 숫자로 기온을 입력받음 | 없음 | 사용자가 입력한 기온 (`temp`) |
| `recommend_clothes(temp)` | 기온에 맞는 옷을 2차원 리스트에서 찾아 추천 | 오늘 기온 (`temp`) | 조건에 맞는 옷차림 화면 출력 |
| `main()` | 위의 함수들을 순서대로 실행하는 총괄 매니저 | 없음 | 없음 |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

예) 사용자의 예산보다 가격이 낮은 항목만 추천한다.

내 계획:입력받은 오늘 기온이 옷의 최저 기온 이상이고 최고 기온 이하인지 확인할 때 `if min_temp <= temp <= max_temp:` 조건문을 사용한다.


### 반복문을 사용할 부분

예) 2차원 리스트의 모든 항목을 하나씩 검사한다.

내 계획:2차원 리스트 `outfits`에 저장된 여러 개의 옷 데이터를 첫 번째 줄부터 마지막 줄까지 하나씩 꺼내어 검사할 때 `for row in outfits:` 반복문을 사용한다.


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 사용자가 설정된 기온 범위(예: 영하 20도 또는 영상 40도 등)를 벗어나는 아주 극단적인 온도를 입력했을 때, 리스트에 조건이 맞는 옷이 없어서 아무것도 출력되지 않을 수 있다.
2. 숫자가 아닌 문자(예: "추워")를 입력했을 때 에러가 발생할 수 있다.

---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. 수행평가 필수 조건인 2차원 리스트 구조를 설계하는 방법과 복잡한 코드를 기능별 함수로 깔끔하게 분리하는 구조에 대해 도움을 받았다.
