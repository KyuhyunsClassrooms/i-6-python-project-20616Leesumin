1. 프로젝트 주제내가 만들 프로그램의 주제: 사용자 맞춤형 패션 스타일 점수제 추천 프로그램
2. 문제 상황이 프로그램이 해결하려는 문제를 설명하세요.내 설명: 매일 아침 등교할 때나 주말에 특별한 외출, 데이트, 혹은 발표 수업이 있을 때 "오늘 어떤 옷을 입어야 하지?"라며 선택장애를 겪는 문제가 자주 발생한다. 이를 해결하기 위해 사용자가 원하는 스타일, 현재 계절, 옷을 입고 갈 구체적인 상황을 입력하면 프로그램 내부에 저장된 패션 데이터 세트와 대조하여 가장 잘 어울리는 옷차림을 골라주는 프로그램을 만들고자 한다. 특히 조건이 완벽히 일치하지 않더라도 '일치 점수'를 계산하여 차선책까지 유연하게 추천해 주는 시스템을 모델링하고자 한다.
3. 입력-처리-출력 구조입력사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?사용자가 키보드로 입력하는 '원하는 스타일 (문자열: 캐주얼/단정/러블리/스트릿)'사용자가 키보드로 입력하는 '현재 계절 (문자열: 봄/여름/가을/겨울)'사용자가 키보드로 입력하는 '외출 상황/목적 (문자열: 학교/외출/데이트/발표)'처리입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?가중치 점수 계산: 2차원 리스트 fashion에 저장된 8개의 옷 코디 데이터를 반복문으로 순회하면서, 사용자가 입력한 세 가지 조건(스타일, 계절, 상황)과 비교하여 일치할 때마다 해당 옷 항목의 점수(score)를 1점씩 차곡차곡 누적한다.우선순위 정렬 순회: 어려운 정렬 함수 대신 알고리즘적 사고를 적용하여, for 반복문을 2번 나누어 실행한다. 첫 번째 루프에서는 3개 조건이 만점인 3점짜리 항목을 먼저 수집하고, 두 번째 루프에서는 2점짜리 항목을 나중에 수집하여 결과 리스트(results)에 순서대로 누적하는 방식으로 내림차순 정렬을 구현한다.출력프로그램이 최종적으로 무엇을 출력하는가?일치 점수가 높은 순서(3점 -> 2점)대로 매칭된 추천 옷차림의 이름, 스타일, 계절, 상황, 그리고 세부 코디 팁 설명을 화면에 깔끔하게 출력한다.만약 사용자가 입력한 조건과 일치하는 항목(2점 이상)이 단 하나도 없다면, 예외 안내 메시지("조건에 맞는 패션 스타일이 없습니다.")를 출력한다.
4. 2차원 리스트 설계내 프로그램에서 사용할 2차원 리스트의 이름: fashion한 행의 의미추천할 수 있는 '패션 코디 세트 1개'의 전체 묶음 정보 (옷 조합 이름, 대표 스타일, 어울리는 계절, 적합한 상황, 코디 설명)각 열의 의미열 번호의미실제 예시0번 열추천 옷차림 이름"후드티 + 청바지", "니트 + 슬랙스"1번 열패션 스타일 분류"캐주얼", "단정", "러블리", "스트릿"2번 열어울리는 계절"봄", "여름", "가을", "겨울"3번 열착용할 상황 및 장소"학교", "외출", "데이트", "발표"4번 열상세 코디 팁 설명"편하고 활동하기 좋은 스타일"
5. 함수 설계함수 이름역할입력값반환값 또는 출력show_intro()프로그램 시작 타이틀과 사용 안내 문구 출력없음화면 텍스트 출력get_user_input()사용자에게 스타일, 계절, 상황을 키보드로 입력받음없음사용자가 입력한 세 가지 문자열 변수calculate_score()하나의 옷 데이터와 사용자 입력값을 비교해 매칭 점수 계산옷 데이터 1행, 입력값 3개누적된 점수 정수 (score)find_best_fashion()전체 데이터를 돌며 3점 항목과 2점 항목을 순서대로 수집사용자 입력값 3개정렬이 끝난 결과 리스트 (results)print_result()수집된 추천 결과를 서식에 맞춰 화면에 출력정렬된 결과 리스트최종 조건별 추천 결과 화면 출력main()위의 함수들을 순서대로 호출하는 총괄 제어 매니저없음없음
6. 사용할 제어구조조건문을 사용할 부분calculate_score 함수 내에서 옷의 데이터 항목들과 사용자 입력값이 일치하는지 개별적으로 비교하여 가중치를 부여할 때 if item[1] == style: 등의 독립 조건문을 사용한다.find_best_fashion에서 점수가 정확히 3점인지 혹은 2점인지 판별하여 리스트에 필터링할 때 if score == 3: 조건문을 사용한다.print_result에서 결과 리스트의 크기가 0인지 아닌지 판별하여 예외 처리를 할 때 if len(results) == 0: 구조를 활용한다.반복문을 사용할 부분2차원 리스트 fashion에 들어있는 8개의 패션 데이터를 처음부터 끝까지 순서대로 하나씩 꺼내어 점수 계산 함수에 전달할 때 for item in fashion: 반복문을 사용한다.print_result 함수 내에서 최종 선별된 결과 리스트 속 옷들을 하나씩 화면에 출력하기 위해 for item in results: 루프를 사용한다.
7. 예상되는 예외 상황사용자가 단어를 입력할 때 실수로 띄어쓰기를 포함해 입력하거나(예: "봄 "), 데이터베이스에 정의되지 않은 완전히 엉뚱한 스타일명(예: "우주인스타일")을 입력할 때 일치 점수가 미달하여 추천 결과가 나오지 않을 수 있다.모든 옷의 매칭 점수가 2점 미만(0점 또는 1점)으로 나와 추천 결과 바구니가 완전히 비어버리는 경계 상황이 발생할 수 있다.
8. Gemini에게 도움받고 싶은 부분단순한 1:1 차단형 매칭 구조에서 벗어나 일부 조건만 맞아도 차선책을 유연하게 제안할 수 있는 '누적 점수제 가중치 계산 알고리즘' 아이디어를 얻고자 했다.고등학교 정보 교과 과정에서 학습하는 for 반복문과 if 조건문 체계만을 응용하여, 어려운 내장 정렬 함수를 쓰지 않고도 3점과 2점 항목을 순차 배치하는 '다중 순회 방식의 정렬 알고리즘' 구현 과정에 대해 조언을 받았다.