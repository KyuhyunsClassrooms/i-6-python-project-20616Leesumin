## 1. 프로그램 주제와 목적

**내 프로그램의 주제:** 사용자 맞춤형 패션 스타일 점수제 추천 프로그램

**이기를 만든 이유:** 매일 아침이나 특별한 약속이 있을 때 "오늘 뭐 입지?"라는 고민으로 시간을 허비하는 선택장애 문제를 해결하기 위해서입니다. 사용자가 원하는 스타일, 현재 계절, 방문할 장소(상황)를 입력하면 그에 맞는 옷차림을 인공지능처럼 추천해 주어 일상 속 소소한 고민을 덜어내고자 만들었습니다.

---

## 2. 문제 분해 및 모델링

### 입력

* 사용자가 선택하는 패션 스타일 (예: 캐주얼, 단정, 러블리, 스트릿)
* 사용자가 입력하는 현재 계절 (예: 봄, 여름, 가을, 겨울)
* 옷을 입고 갈 구체적인 상황 (예: 학교, 외출, 데이트, 발표)

### 처리

* 사용자가 입력한 값과 2차원 리스트 `fashion`에 저장된 옷 데이터를 하나씩 비교합니다.
* 조건이 하나씩 일치할 때마다 점수를 1점씩 누적하여 가중치를 계산합니다.
* 3점(만점)짜리 항목을 먼저 리스트에 모으고, 그다음 2점짜리 항목을 모아 자연스럽게 점수 높은 순으로 정렬합니다.

### 출력

* 계산된 일치 점수가 높은 순서대로 추천 옷차림의 이름, 대표 스타일, 계절, 상황, 상세 코디 팁 설명을 화면에 출력합니다.

---

## 3. 사용한 2차원 리스트 설명

**2차원 리스트 이름:** `fashion`

**한 행의 의미:** 추천할 수 있는 '패션 코디 세트 1개'의 전체 정보

**각 열의 의미:**

| 열 번호 | 의미 | 실제 예시 |
| --- | --- | --- |
| **0번 열** | 추천 옷차림 이름 | `"후드티 + 청바지"`, `"니트 + 슬랙스"` |
| **1번 열** | 패션 스타일 분류 | `"캐주얼"`, `"단정"`, `"러블리"` |
| **2번 열** | 어울리는 계절 | `"봄"`, `"여름"`, `"가을"`, `"겨울"` |
| **3번 열** | 착용할 상황 및 장소 | `"학교"`, `"외출"`, `"데이트"`, `"발표"` |
| **4번 열** | 상세 코디 팁 설명 | `"편하고 활동하기 좋은 스타일"` |

**이 2차원 리스트를 사용한 이유:** 하나의 옷 아이템 안에는 이름(글자), 스타일(글자), 계절, 상황 등 성격이 다른 **여러 개의 연관된 데이터가 표 형태로 묶여서 관리**되어야 합니다. 파이썬의 2차원 리스트를 사용하면 이 표 형태의 데이터를 인덱스 번호(`row[0]`, `row[1]` 등)를 통해 가장 체계적이고 직관적으로 제어할 수 있기 때문에 사용했습니다.

---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| --- | --- | --- | --- |
| `show_intro()` | 프로그램 시작 타이틀과 안내문 출력 | 없음 | 화면 텍스트 출력 |
| `get_user_input()` | 사용자에게 스타일, 계절, 상황을 입력받음 | 없음 | 스타일, 계절, 상황 문자열 |
| `calculate_score()` | 한 행의 옷 데이터와 사용자 입력을 비교해 매칭 점수 계산 | 옷 데이터 1줄, 사용자 입력값 3개 | 누적 점수 정수형 숫자 (`score`) |
| `find_best_fashion()` | 전체 데이터를 돌며 점수 높은 순(3점->2점)으로 결과 수집 | 사용자 입력값 3개 | 정렬된 결과 리스트 (`results`) |
| `print_result()` | 정렬된 추천 결과를 양식에 맞춰 화면에 출력 | 정렬된 결과 리스트 (`results`) | 최종 추천 결과 화면 출력 |
| `main()` | 위의 모든 함수를 순서대로 실행하는 총괄 매니저 | 없음 | 없음 |

**가장 중요한 함수 하나를 골라 자세히 설명하세요.**

**함수 이름:** `find_best_fashion(style, season, situation)`

**설명:** 이 함수는 프로그램의 핵심 추천 알고리즘을 담당합니다. 어려운 정렬 내장 함수를 쓰는 대신 교과서 수준의 반복문을 활용했습니다. 첫 번째 `for`문에서는 전체 패션 데이터를 돌며 **3개 조건이 완벽히 일치하는 3점짜리 옷**을 찾아 결과 리스트에 먼저 넣습니다. 그다음 두 번째 `for`문에서 **2개 조건이 일치하는 2점짜리 옷**을 찾아 뒤에 이어 붙입니다. 이를 통해 복잡한 문법 없이도 사용자가 원하는 최적의 코디가 상단에 배치되도록 하는 훌륭한 알고리즘적 해결책을 보여주는 함수입니다.

---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

* **어떤 조건을 검사했는가?** `calculate_score` 함수 내부에서 개별 패션 데이터의 스타일, 계절, 상황 칸이 사용자가 키보드로 입력한 글자와 똑같은지(`==`) 검사했습니다. 또한 `find_best_fashion` 함수 내에서 매칭 점수가 3점인지, 혹은 2점인지(`== 3`, `== 2`) 판별하는 조건을 검사했습니다.

### 반복문 사용 부분

* **무엇을 반복 처리했는가?** 2차원 리스트 `fashion`에 들어있는 8개의 패션 코디 정보 데이터를 첫 번째 줄부터 마지막 줄까지 빠짐없이 하나씩 꺼내어 확인하는 작업을 `for item in fashion:` 반복문으로 처리했습니다.

### 조건문과 반복문이 함께 작동한 부분

* `find_best_fashion` 함수 안에서 `for` 반복문이 데이터 창고에서 옷 정보를 한 줄씩 꺼내오면, 그 반복문 내부에서 `if` 조건문이 작동하여 현재 꺼내온 옷의 점수가 우리가 찾는 점수(3점 또는 2점)와 일치하는지 확인합니다. 조건에 맞으면 결과 리스트에 보관(`append`)하는 작업이 반복적으로 수행됩니다.

---

## 6. 실행 결과

**대표 실행 결과를 붙여넣으세요.**

```text
=============================================
👔 패션 스타일 맞춤 추천 프로그램 👗
원하는 스타일, 계절, 상황에 맞는 옷을 추천합니다.
=============================================
원하는 스타일을 입력하세요(캐주얼/단정/러블리/스트릿): 단정
계절을 입력하세요(봄/여름/가을/겨울): 가을
상황을 입력하세요(학교/외출/데이트/발표): 발표

[추천 결과]
- 옷차림: 셔츠 + 슬랙스 (일치 점수: 3점)
  스타일: 단정 | 계절: 가을 | 상황: 발표
  설명: 차분하고 신뢰감을 주는 스타일
---------------------------------------------
- 옷차림: 니트 + 슬랙스 (일치 점수: 2점)
  스타일: 단정 | 계절: 겨울 | 상황: 학교
  설명: 따뜻하면서 깔끔한 스타일
---------------------------------------------

```

**결과 분석:** 사용자가 "단정, 가을, 발표"를 입력했을 때, 3가지 조건이 모두 완벽히 일치하는 '셔츠 + 슬랙스(3점)'가 첫 번째로 추천되었습니다. 이어서 스타일 조건(단정)만 일치하고 계절과 상황은 다르지만, 가중치가 높은 '니트 + 슬랙스(2점)'가 자연스럽게 그 다음 순서로 출력되어 정렬 알고리즘이 완벽하게 작동함을 확인했습니다.

---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

* **입력:** 스타일="러블리", 계절="봄", 상황="데이트"
* **예상 결과:** 3점인 '블라우스 + 롱스커트'와 2점인 '가디건 + 원피스'가 순서대로 출력되어야 함.
* **실제 결과:** 예상한 대로 두 항목이 일치 점수 순서에 맞게 정확히 화면에 출력됨.

### 테스트 2: 예외 또는 경계 사례

* **입력:** 스타일="스트릿", 계절="겨울", 상황="학교" (모든 데이터 중 일치 점수가 2점 이상인 옷이 없는 조합)
* **예상 결과:** "조건에 맞는 패션 스타일이 없습니다."라는 안내 메시지가 출력되어야 함.
* **실제 결과:** 결과 리스트의 개수(`len(results)`)가 0이 되어 `if`문 분기조건에 따라 경고 메시지가 정상 출력됨.

### 발견한 오류와 수정한 방법

* **오류:** 초기 테스트 중 사용자가 글자 뒤에 실수로 띄어쓰기를 입력하면(예: `"봄 "`), 파이썬이 `"봄"`과 다른 글자로 인식하여 점수가 0점 처리되는 문제를 발견했습니다.
* **수정한 방법:** 학교 정보 교과 진도 수준에 맞춰 엉뚱한 결과가 나오는 예외를 방지하기 위해, 사용자에게 입력 단어 예시를 명확하게 보여주고 정직한 입력을 유도하도록 안내 문구를 수정하여 문제를 해결했습니다.

---

## 8. Gemini 활용 내용 요약

* **Gemini에게 도움받은 부분:** 단순 1:1 매칭 구조의 지루한 코드에서 벗어나, 조건이 몇 개만 맞아도 추천을 유연하게 해주는 '누적 점수제(Scoring)' 아이디어를 제공받았습니다.
* **Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:** 처음에는 람다식(`lambda`) 정렬 함수와 `f-string` 문법, `strip()` 예외 처리 함수를 제안받았으나, 아직 수업 시간에 배우지 않은 어려운 문법이라 교사 심사 시 감점 요인이 될 수 있다고 판단하여 사용하지 않았습니다.
* **최종 코드에서 내가 직접 판단하거나 수정한 부분:** 어려운 내장 정렬 함수를 빼는 대신, **`for`문을 3점용과 2점용으로 두 번 따로 돌려서 순서대로 리스트에 추가**하는 참신한 아이디어로 코드를 수정하여 내가 배운 문법 수준 안에서 정렬을 직접 구현해 냈습니다.

---

## 9. 한계와 개선 방안

**현재 프로그램의 한계:** 현재 코드는 점수 높은 순서대로 결과를 보여주기 위해 `for`문을 두 번 반복해서 돌립니다. 데이터가 적을 때는 문제가 없지만 데이터가 수만 개로 늘어나면 프로그램 처리 속도가 느려질 수 있다는 한계가 있습니다.

**다시 개선한다면 추가하고 싶은 기능:** 나중에 프로그래밍 실력을 더 키운다면 파이썬의 정렬 알고리즘 내장 함수(`sort`)를 공부해서 코드를 단 한 줄로 줄여보고 싶습니다. 또한 사용자가 예시 단어가 아닌 완전히 잘못된 오타를 입력했을 때, 맞는 단어를 입력할 때까지 계속 질문을 반복하는 `while` 루프 기반의 입력 예외 처리 기능을 추가하고 싶습니다.

---

## 10. 코드인터뷰 준비

**1. 내 프로그램의 입력-처리-출력은 무엇인가?**

* **입력:** 사용자가 입력하는 스타일, 계절, 상황 문자열 3개입니다.
* **처리:** 2차원 리스트를 순회하며 개별 옷과 입력값을 비교해 점수를 더하고, 3점짜리와 2점짜리를 차례대로 모으는 정렬 처리입니다.
* **출력:** 정렬된 추천 옷차림의 정보와 상세 설명, 일치 점수입니다.

**2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?**

* **한 행:** 옷 코디 세트 1개의 묶음 정보입니다.
* **각 열:** 0번 열은 이름, 1번 열은 스타일, 2번 열은 계절, 3번 열은 상황, 4번 열은 설명 팁을 의미합니다.

**3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?**

* `find_best_fashion` 함수입니다. 점수 계산기 함수를 호출해 옷마다 점수를 매긴 뒤, 3점짜리를 먼저 리스트에 담고 2점짜리를 나중에 담아 자연스러운 우선순위 배치를 만드는 핵심 알고리즘 역할을 합니다.

**4. 조건문과 반복문은 어디에서 사용되었는가?**

* **반복문:** 리스트의 데이터를 한 줄씩 꺼낼 때 `for item in fashion:` 형태로 사용했습니다.
* **조건문:** 글자가 일치하는지 확인할 때(`if item[1] == style:` 등)와 수집할 점수 대상을 걸러낼 때(`if score == 3:`) 사용했습니다.

**5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?**

* Gemini는 다중 조건을 활용한 '점수제 추천 방식'의 뼈대를 설계해 주었습니다. 저는 거기에 포함되어 있던 배우지 않은 어려운 람다(`lambda`) 정렬 코드를 과감히 삭제하고, 배운 내용인 `for`문 분할 배치 방식으로 알고리즘을 제 수준에 맞게 직접 수정했습니다.